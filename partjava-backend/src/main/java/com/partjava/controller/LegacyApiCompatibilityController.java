package com.partjava.controller;

/**
 * Former Next.js API compatibility endpoints have been promoted into formal Java controllers.
 * This class is intentionally kept without request mappings so old references fail at compile-time
 * if someone tries to add compatibility behavior back here.
 */
public final class LegacyApiCompatibilityController {
    private LegacyApiCompatibilityController() {
    }
}
